title: "Tools — Analyse de dépendances .NET"
description: "Scripts PowerShell pour analyser, visualiser et auditer les dépendances entre projets .NET (.csproj)."
remote_theme: just-the-docs/just-the-docs@v0.10.1

url: "https://niccou.github.io"
baseurl: "/tools"

color_scheme: light
search_enabled: true

aux_links:
  "Voir sur GitHub":
    - "https://github.com/niccou/tools"
aux_links_new_tab: true

footer_content: "Scripts PowerShell — Analyse de dépendances .NET"

plugins:
  - jekyll-remote-theme
  - jekyll-seo-tag
